{

    "mode":"${SQUISHY_ENV|develop}",
    "host":"${SQUISHY_HOST|localhost}",
    "port":"${SQUISHY_PORT|80}",
    "log":{
        "output": "log_file",
        "mode":"${SQUISHY_LOG_MODE|DEBUG}"
    },
    "db":{
        "driver":"postgress",
        "path":"${SQUISHY_DB_HOST}"
    }
}